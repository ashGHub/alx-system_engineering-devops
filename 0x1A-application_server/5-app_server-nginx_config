# Set this outside server {}
map $uri $proxy_pass_url {
    ~^/$ http://127.0.0.1:5003/2-hbnb;
    default http://127.0.0.1:5003$uri;
}

# Set this inside the server {}
location / {
    proxy_pass $proxy_pass_url;
}
